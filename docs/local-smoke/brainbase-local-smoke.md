# Brainbase Local MVP

The team decided to run Brainbase extraction and embeddings locally with Ollama during the MVP.

The goal is to turn local documents into cited company knowledge that agents can retrieve.

All extracted knowledge must enter the review queue as proposed before it can be confirmed.

The Local Knowledge Slice project implements document ingestion, structured extraction, review, and cited retrieval.
